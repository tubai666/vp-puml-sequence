interface:
  display_name: "VP PUML Sequence"
  short_description: "Convert requirements or PUML to VP."
  default_prompt: "Use $vp-puml-sequence to derive PUML sequence diagrams if needed, convert them into a Visual Paradigm .vpp project, and export matching PNG/SVG images."

policy:
  allow_implicit_invocation: true
